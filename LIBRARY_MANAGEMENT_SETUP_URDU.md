# لائبریری نظام کی تنصیب

1. Supabase کے SQL Editor میں `supabase/library-management.sql` مکمل چلائیں۔
2. پورٹل دوبارہ چلائیں: `npm.cmd run dev -- --webpack -p 3001`
3. ایڈمن لاگ اِن کے بعد `http://localhost:3001/admin/library` کھولیں۔
4. پہلے کتاب، مصنف، موضوع، شیلف، نسخوں کی تعداد اور شناختی کوڈ درج کریں۔
5. پھر دستیاب نسخہ طالب علم یا استاد کو مقررہ واپسی تاریخ کے ساتھ جاری کریں۔

ہر نسخے کا شناختی نمبر منفرد ہونا چاہیے۔ خفیہ `.env.local` فائل کسی کے ساتھ شیئر نہ کریں۔
