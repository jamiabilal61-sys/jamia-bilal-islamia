# امتحانات اور نتائج — تنصیب

1. Supabase کے SQL Editor میں `supabase/examinations-results.sql` مکمل چلائیں۔
2. Terminal لازماً `portal` فولڈر میں کھولیں۔
3. یہ کمانڈ چلائیں: `npm.cmd install`
4. پھر: `npm.cmd run dev -- --webpack -p 3001`
5. صفحہ: `http://localhost:3001/admin/exams`

پہلے امتحان اور مضامین بنائیں، پھر امتحان منتخب کرکے نمبر درج کریں۔ مکمل نمبر محفوظ ہونے کے بعد رزلٹ کارڈ اور پوزیشن فہرست پرنٹ کی جا سکتی ہے۔
