# مرکزی ڈیش بورڈ اور عہدوں کے اختیارات

## تنصیب

Supabase کے SQL Editor میں یہ فائل چلائیں:

`supabase/dashboard-role-access.sql`

اس کے بعد سرور دوبارہ چلائیں:

```powershell
npm.cmd install
npm.cmd run dev -- --webpack -p 3001
```

مرکزی ڈیش بورڈ:

`http://localhost:3001/admin`

صارفین اور اختیارات:

`http://localhost:3001/admin/users`

## پہلا صارف

ابتدائی `.env.local` والے مرکزی ایڈمن سے لاگ اِن کریں۔ پھر **صارفین و اختیارات** میں مدیر، اکاؤنٹنٹ، استاد، وارڈن، لائبریرین یا کلرک کا الگ اکاؤنٹ بنائیں۔ ہر اکاؤنٹ کے لیے کم از کم آٹھ حروف کا منفرد پاس ورڈ رکھیں۔

مینو اور API دونوں سطحوں پر اجازت نافذ ہے۔ کسی صارف کو غیر متعلقہ صفحے کا براہِ راست لنک دینے سے بھی رسائی نہیں ملے گی۔
