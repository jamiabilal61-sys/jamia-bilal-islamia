# Word اور Excel سے طلبہ درآمد

1. Supabase SQL Editor میں `supabase/student-import.sql` چلائیں۔
2. پورٹل چلائیں: `npm.cmd run dev -- --webpack -p 3001`
3. `http://localhost:3001/admin/student-import` کھولیں۔

## مطلوبہ کالم
لازمی: `طالب علم کا نام`، `والد کا نام`، `شعبہ`۔ اختیاری: `موبائل نمبر`، `طالب علم نمبر`، `جماعت`، `پتہ`، `تاریخ پیدائش`، `جنس`، `ای میل`، `تاریخ داخلہ`۔

Word میں ریکارڈ باقاعدہ Table کی صورت میں اور پہلی قطار میں یہی عنوانات رکھیں۔ Excel/CSV میں پہلی Row عنوانات کی ہو۔ پہلے Preview بنتا ہے؛ غلط قطاریں واضح ہوتی ہیں اور منظوری کے بغیر کوئی ریکارڈ محفوظ نہیں ہوتا۔ طالب علم نمبر یا موبائل پہلے موجود ہو تو وہ ریکارڈ دوبارہ درآمد نہیں کیا جاتا۔
