"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function PortalLogin(){const router=useRouter(),[username,setUsername]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState(""),[busy,setBusy]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage("");const r=await fetch("/api/portal/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password})});const d=await r.json();setBusy(false);if(d.success){router.replace("/portal");router.refresh()}else setMessage(d.message)}
 return <form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-2xl" dir="rtl"><div className="text-center"><img src="/images/logo.png" alt="جامعہ بلال اسلامیہ" className="mx-auto h-24 w-24 object-contain"/><h1 className="mt-4 text-3xl font-black text-blue-950">والدین و طالب علم پورٹل</h1><p className="mt-2 text-slate-500">حاضری، فیس، نتائج اور تعلیمی ریکارڈ</p></div>{message&&<p className="mt-5 rounded-xl bg-red-50 p-3 text-red-700">{message}</p>}<div className="mt-7 space-y-4"><label className="block font-bold">صارف نام<input className="mt-2 w-full rounded-xl border p-3 font-sans" dir="ltr" value={username} onChange={e=>setUsername(e.target.value)} required/></label><label className="block font-bold">پاس ورڈ<input type="password" className="mt-2 w-full rounded-xl border p-3 font-sans" dir="ltr" value={password} onChange={e=>setPassword(e.target.value)} required/></label><button disabled={busy} className="w-full rounded-xl bg-blue-900 py-3 font-bold text-white disabled:opacity-60">{busy?"لاگ اِن ہو رہا ہے…":"لاگ اِن کریں"}</button></div><p className="mt-5 text-center text-sm text-slate-500">صارف نام اور پاس ورڈ جامعہ کے دفتر سے حاصل کریں۔</p></form>}
