# اساتذہ اور ٹائم ٹیبل — تنصیب

1. Supabase کے SQL Editor میں `supabase/faculty-timetable.sql` مکمل چلائیں۔
2. `.env.local` پہلے کی طرح محفوظ رہنے دیں؛ اسے ZIP سے تبدیل نہ کریں۔
3. PowerShell میں `portal` فولڈر کے اندر یہ کمانڈ چلائیں:

```powershell
npm.cmd install
npm.cmd run dev -- --webpack -p 3001
```

4. `http://localhost:3001/admin/faculty` کھولیں۔
5. پہلے اساتذہ، ان کے قابل تدریس مضامین اور دستیاب دن محفوظ کریں۔ مضمون کی املا دونوں جگہ ایک جیسی رکھیں۔
6. پھر ہر جماعت کے مضامین اور ہفتہ وار پیریڈ درج کریں۔ آخر میں خودکار ٹائم ٹیبل بنائیں۔

اگر کسی مضمون کے لیے موزوں استاد، دستیاب وقت یا بوجھ کی گنجائش نہ ہو تو نظام اسے زبردستی غلط جگہ نہیں لگائے گا بلکہ Conflict رپورٹ دکھائے گا۔
